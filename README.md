
  # Memory game design

  This is a code bundle for Memory game design. The original project is available at https://www.figma.com/design/BP2n7kYedchaiItospPWb7/Memory-game-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  